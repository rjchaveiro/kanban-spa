{
  "name": "kanban-spa",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cross-env": "^5.2.0",
    "node-sass": "^4.11.0",
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "2.1.5"
  },
  "scripts": {
    "start": "cross-env NODE_PATH=src/:src/app/:src/ react-scripts start",
    "build": "cross-env NODE_PATH=src/:src/app/:src/ react-scripts build",
    "test": "cross-env NODE_PATH=src/:src/app/:src/ react-scripts test",
    "eject": "cross-env NODE_PATH=src/:src/app/:src/ react-scripts eject",
    "flow": "flow"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "prettier": "^1.16.4"
  }
}
